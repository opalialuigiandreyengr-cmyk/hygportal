# Supabase

This folder is reserved for Supabase migrations, seed data, and future Edge Functions.

Planned structure:

```text
migrations/
seed/
functions/
```

The database owns the approval engine and access rules. The mobile app should not decide who approves a request.

